{
  "name": "satteri-plugins-workspace",
  "private": true,
  "repository": {
    "type": "git",
    "url": "github:baka-gourd/satteri-plugins"
  },
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "test": "pnpm -r test",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "ci:publish": "pnpm -r publish --provenance --no-git-checks"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@types/node": "^26.0.0",
    "oxfmt": "^0.56.0",
    "oxlint": "^1.71.0",
    "oxlint-tsgolint": "^0.23.0",
    "satteri": "^0.9.2",
    "tsdown": "^0.22.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  },
  "packageManager": "pnpm@11.8.0"
}
