{
  "name": "satteri-plugins-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.5.2",
  "repository": {
    "url": "github:baka-gourd/satteri-plugins",
    "type": "git"
  },
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "test": "pnpm -r test",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "ci:publish": "pnpm -r --workspace-concurrency=1 publish --access public --provenance --no-git-checks"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@types/node": "^25.9.2",
    "oxfmt": "^0.54.0",
    "oxlint": "^1.69.0",
    "oxlint-tsgolint": "^0.23.0",
    "satteri": "^0.8.1",
    "tsdown": "^0.22.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
